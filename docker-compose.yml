version: "3"
services:
  node:
    image: node:18.3.0
    entrypoint: /app
    command: git clone https://github.com/fermartz/company-express.git && npm install && node index.js
    container_name: express-company-service
    ports:
      - "3000:3000"